{
  "schema_version": 1,
  "scenarios": [
    {
      "id": "baseline_default_gps",
      "gps": {
        "model": "NEO-M8 default concurrent constellation",
        "acquisition_time_us": 0,
        "poll_period_us": 5000,
        "duration_us": 2500000
      },
      "i2c_mode": "physical",
      "button_physically_pressed": true
    },
    {
      "id": "i2c_attempt_default_gps",
      "gps": {
        "model": "NEO-M8 default concurrent constellation",
        "acquisition_time_us": 0,
        "poll_period_us": 5000,
        "duration_us": 2500000
      },
      "i2c_mode": "physical",
      "button_physically_pressed": true
    },
    {
      "id": "i2c_reachable_default_gps",
      "gps": {
        "model": "NEO-M8 default concurrent constellation",
        "acquisition_time_us": 0,
        "poll_period_us": 5000,
        "duration_us": 2500000
      },
      "i2c_mode": "physical",
      "button_physically_pressed": true
    },
    {
      "id": "normal_default_gps",
      "gps": {
        "model": "NEO-M8 default concurrent constellation",
        "acquisition_time_us": 0,
        "poll_period_us": 5000,
        "duration_us": 2500000
      },
      "i2c_mode": "physical",
      "button_physically_pressed": true
    },
    {
      "id": "normal_default_gps_sanitized",
      "gps": {
        "model": "NEO-M8 default concurrent constellation",
        "acquisition_time_us": 0,
        "poll_period_us": 5000,
        "duration_us": 2500000
      },
      "i2c_mode": "physical",
      "button_physically_pressed": true,
      "sanitizer": "address"
    },
    {
      "id": "complete_probe_scan",
      "gps": {"injection": "$GNRMC valid fix"},
      "i2c_mode": "physical",
      "button_gate": "pre-P7 released level",
      "expected": "40 rows execute and one raw output remains selected"
    },
    {
      "id": "complete_probe_scan_sanitized",
      "gps": {"injection": "$GNRMC valid fix"},
      "i2c_mode": "physical",
      "button_gate": "pre-P7 released level",
      "sanitizer": "undefined",
      "expected": "narrow mask counterfactual fails at shift exponent 32"
    },
    {
      "id": "logical_mapping_pair",
      "board_fixture": "source-derived schematic-ideal J3-to-U4 routing",
      "harness": [[20, 21]],
      "expected": "logical row 20 observes logical bit 21 through raw bits 24 and 25"
    },
    {
      "id": "verdict_all_rows",
      "board_fixture": "source-derived schematic-ideal J3-to-U4 routing",
      "expected": "an all-row oracle match passes and an exactly-one-row match fails"
    },
    {
      "id": "expected_matrix_passive_closure",
      "board_fixture": "source-declared harness edges with schematic-ideal J3-to-U4 routing",
      "expected": "the passive component closure changes exactly 36 rows; the declared harness fails with sparse rows and passes with closed rows",
      "limitation": "the repository contains no independent external-harness specification, so closure preserves the source-declared edge graph rather than proving that graph is the intended product harness"
    },
    {
      "id": "harness_exhaustive_campaign",
      "board_fixture": "P9 generated firmware with schematic-ideal J3-to-U4 routing",
      "bounds": {
        "declared_baseline": 1,
        "single_channel_opens": 40,
        "two_channel_opens": 780,
        "all_pair_additions": 780,
        "seeded_mixed_open_short": 256,
        "synthetic_three_pin_components": 128,
        "synthetic_four_pin_components": 128,
        "seed": 3237998146
      },
      "expected": "firmware rows, verdict, log, and persistent status agree with the independent component oracle for every bounded scenario",
      "output": "build/blocker_peeling/exposure_matrix.csv"
    },
    {
      "id": "button_press_hold_release",
      "board_fixture": "source-derived schematic-ideal J3-to-U4 routing",
      "sequence": ["release", "press", "hold", "release", "press"],
      "expected": "exactly two sessions"
    },
    {
      "id": "led_status_closed_loop",
      "board_fixture": "KiCad-derived D3 connectivity plus generated current-limited overlay",
      "states": ["BUSY", "GOOD", "FAILED"],
      "expected": "ngspice current selects blue, green, and red respectively only when firmware modes, power path, resistors, and color mapping are all repaired"
    },
    {
      "id": "led_failed_status_persistence",
      "board_fixture": "KiCad-derived D3 connectivity plus generated current-limited overlay",
      "sequence": ["failed result", "held-button idle loop"],
      "expected": "FAILED remains physical red until a new test starts; the leave-one-out variant changes to GOOD without a new result"
    },
    {
      "id": "sd_partial_record",
      "board_fixture": "source-derived schematic-ideal J3-to-U4 routing",
      "sd_capacity_bytes": [17, 24, 25],
      "expected": "whole-call result and CRLF write failures emit a diagnostic, while a complete CRLF record does not",
      "limitation": "Teensy SdFat reports each write call as full length or zero and can defer media failure to close/sync; the void File.close API leaves deferred failure outside this repair's observable boundary"
    },
    {
      "id": "peripheral_event_campaign",
      "board_fixture": "P9 generated firmware plus an experimental atomic RMC-validation repair and exact P9 counterfactual",
      "bounds": {
        "nmea_single_message_cases": 11,
        "nmea_state_sequences": 4,
        "nmea_seeded_mutations": 384,
        "button_sequences": 4,
        "uart_sequences": 3,
        "i2c_sequences": 3,
        "sd_sequences": 5,
        "seed": 1163284000
      },
      "oracle_sources": [
        "u-blox 8/M8 protocol specification UBX-13003221 RMC structure and checksum fields",
        "README.md start-button and persistent-result workflow goals",
        "Teensy 1.59 SdFat all-or-zero write-call behavior",
        "runtime transaction traces for model-observation-only fault coverage"
      ],
      "expected": "the atomic RMC variant rejects invalid or partial messages without persistent-state mutation; its exact counterfactual reproduces only A04/BP-M002; other bounded workflows produce no unexplained differential",
      "output": "build/blocker_peeling/peripheral_exposure_matrix.csv",
      "limitations": [
        "the 384-case generated corpus calls process_nmea directly and covers parser semantics, while separate single-message and sequence cases cover loop buffering and framing",
        "button bounce has no selected-part or debounce-time product requirement and is not an admission oracle",
        "I2C fault diagnostics and SD reinsertion policy are observations unless an external product invariant is found",
        "deferred SD close/sync failure remains outside the simulator boundary"
      ]
    },
    {
      "id": "persisted_sanitizer_corpus_campaign",
      "board_fixture": "P9_RMC_VALIDATED generated firmware with immutable checked-in corpus replay",
      "bounds": {
        "valid_rmc": 128,
        "invalid_rmc": 576,
        "post_lock_atomicity": 128,
        "loop_framing": 10,
        "total": 842,
        "seed": 1129467955
      },
      "oracle_sources": [
        "u-blox 8/M8 protocol specification UBX-13003221 RMC structure, status, checksum, time, date, and line terminators",
        "independent deterministic Python case constructor",
        "checked-in case-specific expected lock/time/date outcomes"
      ],
      "expected": "baseline, AddressSanitizer, and UndefinedBehaviorSanitizer replays match every persisted expected state with no sanitizer diagnostic",
      "output": "build/blocker_peeling/persisted_corpus_matrix.csv",
      "limitations": [
        "the campaign targets repaired RMC parser and loop-framing behavior; broader button, I2C, and SD state-sequence metamorphic coverage is a separate expansion",
        "host sanitizer success does not replace Teensy-target compilation or hardware traces"
      ]
    }
  ]
}
